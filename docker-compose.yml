name: voice-key-backend-go

services:
  # app:
  #   container_name: voice-key-app
  #   image: golang:1.22-alpine
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #     - ./.docker/volumes/go-mod-cache:/go/pkg/mod
  #   # command: go run main.go app:start
  #   command: >
  #     sh -c "
  #       go mod tidy &&
  #       go run main.go app:start
  #     "
  #   environment:
  #     - GO_ENV=development
  #   networks:
  #     - voice-key
  #   depends_on:
  #     - db
  #   ports:
  #     - "8080:8080"

  db:
    container_name: voice-key-db
    image: postgres:17.4-alpine3.21
    restart: unless-stopped
    networks:
      - voice-key
    volumes:
      - ./.docker/volumes/postgresql:/var/lib/postgresql/data/pgdata
    ports:
      - 5437:5432
    environment:
      - POSTGRES_DB=voice-key
      - POSTGRES_USER=voice-key
      - POSTGRES_PASSWORD=12345
      - PGDATA=/var/lib/postgresql/data/pgdata

networks:
  voice-key:
    name: voice-key
